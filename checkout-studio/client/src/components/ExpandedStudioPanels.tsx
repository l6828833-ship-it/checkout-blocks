import type { FunnelMode, LogoTreatment, ThemeAtelierPreset } from "@shared/checkoutStudio";
import { BadgeCheck, Check, ChevronRight, CircleDashed, Crown, Eye, Image, Layers3, Lock, MapPin, Palette, Sparkles, Type, Upload } from "lucide-react";
import { useMemo, useState } from "react";

const modes: { name: FunnelMode; note: string; emphasis: string }[] = [
  { name: "Trust-First", note: "Reassure early, clarify care and returns.", emphasis: "Information confidence" },
  { name: "Product Story", note: "Carry product meaning into the order moment.", emphasis: "Order-summary story" },
  { name: "Delivery Confidence", note: "Make timing and care expectations unmistakable.", emphasis: "Shipping clarity" },
  { name: "Minimal Express", note: "Use compact hierarchy for an interruption-free path.", emphasis: "Fast payment flow" },
  { name: "Community & Care", note: "Express values through measured supporting content.", emphasis: "Care and support" },
  { name: "Limited Release", note: "Frame a thoughtful edition without pressure tactics.", emphasis: "Confirmation continuity" },
];

const stageData = [
  { stage: "Contact", goal: "Set trust at the first decision", supported: "Logo treatment · trust/care content", avoid: "Customer-field placement" },
  { stage: "Shipping", goal: "Clarify delivery expectations", supported: "Delivery promise · returns reminder", avoid: "Rate logic or order" },
  { stage: "Payment", goal: "Reinforce confidence", supported: "Secure note · support link", avoid: "Payment-method changes" },
  { stage: "Confirmation", goal: "Extend brand continuity", supported: "Order care · eligible post-purchase module", avoid: "Guaranteed target access" },
];

export function ThemeAtelierPanel({ themes, selectedSlug, brandName, onApply }: { themes: ThemeAtelierPreset[]; selectedSlug: string; brandName: string; onApply: (theme: ThemeAtelierPreset) => void }) {
  const [query, setQuery] = useState("");
  const shown = themes.filter(theme => `${theme.name} ${theme.descriptor} ${theme.funnelMode}`.toLowerCase().includes(query.toLowerCase()));
  return <><div className="flex flex-col gap-4 lg:flex-row lg:items-end lg:justify-between"><div><p className="eyebrow">Theme Atelier</p><h1 className="mt-2 text-3xl font-semibold tracking-[-0.045em]">A point of view for every checkout.</h1><p className="mt-2 max-w-2xl text-sm leading-6 text-stone-500">Twelve editable funnel systems pair a visual language with intentional checkout moments. Applying a theme changes the simulation only until Shopify validates a real store capability.</p></div><div className="search-field"><Palette className="h-4 w-4" /><input value={query} onChange={event => setQuery(event.target.value)} placeholder="Search theme families" /></div></div><div className="mt-6 grid gap-4 md:grid-cols-2 xl:grid-cols-3">{shown.map(theme => <button key={theme.slug} onClick={() => onApply(theme)} className={`group text-left rounded-2xl border bg-white p-4 transition hover:-translate-y-0.5 hover:shadow-[0_12px_28px_rgba(44,38,28,.09)] ${selectedSlug === theme.slug ? "border-[#8d80e6] ring-2 ring-violet-100" : "border-stone-200"}`}><div className="h-24 rounded-xl p-3" style={{ background: `linear-gradient(135deg, ${theme.tokens.background} 0%, ${theme.tokens.surface} 65%, ${theme.tokens.primary} 190%)`, color: theme.tokens.text }}><div className="flex items-center justify-between"><div className="flex items-center gap-2"><span className="grid h-6 w-6 place-items-center rounded-md" style={{ background: theme.tokens.primary, color: theme.tokens.primaryText }}>{theme.logoTreatment === "Monogram" ? brandName.slice(0, 1) : <Sparkles className="h-3.5 w-3.5" />}</span><span className={`text-xs ${theme.tokens.font === "Editorial" ? "font-serif" : "font-semibold"}`}>{brandName}</span></div><span className="rounded-full border border-black/10 bg-white/50 px-2 py-1 text-[9px] font-bold">{theme.funnelMode}</span></div><div className="mt-5 h-2.5 w-2/3 rounded-full" style={{ background: theme.tokens.primary }} /></div><div className="mt-4 flex items-start justify-between gap-3"><div><h2 className="font-semibold tracking-[-0.02em] text-stone-900">{theme.name}</h2><p className="mt-1 text-xs leading-5 text-stone-500">{theme.descriptor}</p></div>{selectedSlug === theme.slug && <Check className="h-4 w-4 shrink-0 text-[#6556d9]" />}</div><div className="mt-4 flex items-center justify-between border-t border-stone-100 pt-3 text-[11px]"><span className="font-semibold text-[#6556d9]">{theme.mood}</span><span className="text-stone-500">{theme.logoTreatment}</span></div></button>)}</div></>;
}

export function BrandSignaturePanel({ primary, text, brandName, treatment, onChange, onTreatmentChange }: { primary: string; text: string; brandName: string; treatment: LogoTreatment; onChange: (value: string) => void; onTreatmentChange: (value: LogoTreatment) => void }) {
  const [alignment, setAlignment] = useState("Left");
  return <><div><p className="eyebrow">Brand Signature</p><h1 className="mt-2 text-3xl font-semibold tracking-[-0.045em]">Make the brand recognizable before the first field.</h1><p className="mt-2 max-w-2xl text-sm leading-6 text-stone-500">Choose a supported logo presentation and confirm legibility. Exact checkout logo placement is determined by Shopify’s validated configuration.</p></div><div className="mt-6 grid gap-5 xl:grid-cols-[minmax(0,1fr)_330px]"><section className="studio-card overflow-hidden p-0"><div className="p-6" style={{ background: `linear-gradient(135deg, ${primary} 0%, ${primary}cc 65%, #0F0D0B 130%)`, color: text }}><p className="text-[11px] font-bold uppercase tracking-[0.14em] opacity-70">Representative brand lockup</p><div className={`mt-8 flex ${alignment === "Center" ? "justify-center" : alignment === "Right" ? "justify-end" : "justify-start"}`}><div className={`flex items-center gap-3 ${treatment === "Stacked lockup" ? "flex-col gap-2" : ""}`}><span className="grid h-12 w-12 place-items-center rounded-xl bg-white/15 text-xl font-serif italic">{brandName.slice(0, 1).toUpperCase()}</span>{treatment !== "Icon mark" && <span className={treatment === "Wordmark" ? "font-serif text-3xl" : "text-xl font-semibold tracking-[-0.04em]"}>{brandName}</span>}</div></div><p className="mt-10 text-xs opacity-75">Clear space and background contrast are reviewed in this simulation.</p></div><div className="grid gap-3 p-5 sm:grid-cols-3"><Stat label="Contrast" value="9.8:1" good /><Stat label="Clear space" value="24 px" good /><Stat label="Placement" value="To verify" /></div></section><aside className="studio-card p-5"><p className="label">Brand controls</p><label className="mt-4 block text-xs font-semibold text-stone-700">Wordmark<label className="mt-2 block"><input value={brandName} onChange={event => onChange(event.target.value)} className="studio-input" /></label></label><div className="mt-5"><p className="text-xs font-semibold text-stone-700">Treatment</p><div className="mt-2 grid grid-cols-2 gap-2">{(["Wordmark", "Monogram", "Icon mark", "Stacked lockup"] as LogoTreatment[]).map(item => <button key={item} onClick={() => onTreatmentChange(item)} className={`rounded-lg border px-2 py-2 text-xs font-semibold ${treatment === item ? "border-[#8d80e6] bg-violet-50 text-[#6556d9]" : "border-stone-200 text-stone-600"}`}>{item}</button>)}</div></div><div className="mt-5"><p className="text-xs font-semibold text-stone-700">Alignment</p><div className="mt-2 flex gap-2">{["Left", "Center", "Right"].map(item => <button key={item} onClick={() => setAlignment(item)} className={`rounded-lg px-2 py-1.5 text-xs font-semibold ${alignment === item ? "bg-stone-900 text-white" : "bg-stone-100 text-stone-600"}`}>{item}</button>)}</div></div><button onClick={() => undefined} className="mt-6 flex w-full items-center justify-center gap-2 rounded-xl border border-dashed border-stone-300 px-3 py-3 text-xs font-semibold text-stone-500"><Upload className="h-4 w-4" />Upload approved asset after connection</button></aside></div></>;
}

export function FunnelComposerPanel({ activeMode, onMode }: { activeMode: FunnelMode; onMode: (mode: FunnelMode) => void }) {
  const selected = modes.find(mode => mode.name === activeMode) ?? modes[0];
  return <><div><p className="eyebrow">Funnel Composer</p><h1 className="mt-2 text-3xl font-semibold tracking-[-0.045em]">Make each checkout moment feel intentional.</h1><p className="mt-2 max-w-2xl text-sm leading-6 text-stone-500">Select an experience direction, then inspect the eligible visual and content opportunities by stage. Shopify-controlled logic stays untouched.</p></div><div className="mt-6 grid gap-3 md:grid-cols-2 xl:grid-cols-3">{modes.map(mode => <button key={mode.name} onClick={() => onMode(mode.name)} className={`rounded-2xl border p-4 text-left transition ${activeMode === mode.name ? "border-[#8d80e6] bg-violet-50 shadow-sm" : "border-stone-200 bg-white hover:border-stone-300"}`}><div className="flex items-center justify-between"><span className="grid h-8 w-8 place-items-center rounded-lg bg-white text-[#6556d9] shadow-sm"><Sparkles className="h-4 w-4" /></span>{activeMode === mode.name && <Check className="h-4 w-4 text-[#6556d9]" />}</div><p className="mt-4 font-semibold tracking-[-0.02em]">{mode.name}</p><p className="mt-1 text-xs leading-5 text-stone-500">{mode.note}</p></button>)}</div><section className="studio-card mt-6 overflow-hidden p-0"><div className="border-b border-stone-100 bg-[#faf9f6] px-5 py-4"><p className="eyebrow">{selected.name}</p><h2 className="mt-1 text-xl font-semibold tracking-[-0.03em]">{selected.emphasis}</h2></div><div className="grid divide-y divide-stone-100 md:grid-cols-2 md:divide-x md:divide-y-0 xl:grid-cols-4">{stageData.map((stage, index) => <div key={stage.stage} className="p-5"><div className="flex items-center gap-2"><span className="grid h-6 w-6 place-items-center rounded-full bg-[#29251f] text-[10px] font-bold text-white">{index + 1}</span><p className="font-semibold">{stage.stage}</p></div><p className="mt-3 text-xs font-semibold text-stone-700">{stage.goal}</p><p className="mt-2 text-xs leading-5 text-[#466858]">Available when supported: {stage.supported}</p><p className="mt-3 text-xs leading-5 text-stone-500">Never offered: {stage.avoid}</p></div>)}</div></section></>;
}

export function PlacementMapPanel() {
  const zones = [
    { label: "Header logo", state: "To verify", tone: "accent", detail: "Merchant-configurable branding field when Shopify confirms it." },
    { label: "Information care zone", state: "To verify", tone: "accent", detail: "Possible app extension target for concise approved reassurance." },
    { label: "Shipping support zone", state: "To verify", tone: "accent", detail: "Delivery, returns, or sustainability module where an eligible target exists." },
    { label: "Payment reassurance zone", state: "To verify", tone: "accent", detail: "Secure note or support link in a Shopify-approved extension target." },
    { label: "Customer fields", state: "Shopify-controlled", tone: "neutral", detail: "No custom placement, movement, or checkout logic changes." },
    { label: "Order total", state: "Shopify-controlled", tone: "neutral", detail: "No overlay or arbitrary content placement." },
  ];
  return <><div><p className="eyebrow">Placement Map</p><h1 className="mt-2 text-3xl font-semibold tracking-[-0.045em]">Place care, not clutter.</h1><p className="mt-2 max-w-2xl text-sm leading-6 text-stone-500">This map shows representative legal zones. Real targets appear only after an authorized Shopify capability check.</p></div><div className="mt-6 grid gap-5 xl:grid-cols-[minmax(0,1fr)_340px]"><section className="studio-card overflow-hidden p-5"><div className="mx-auto max-w-2xl rounded-2xl border border-stone-200 bg-[#f7f4ee] p-4"><div className="rounded-xl border border-stone-200 bg-white p-4 shadow-sm"><MapZone title="Header logo" state="To verify" /><MapZone title="Contact fields" state="Shopify-controlled" /><MapZone title="Information care zone" state="To verify" /><MapZone title="Shipping support zone" state="To verify" /><MapZone title="Payment reassurance zone" state="To verify" /><MapZone title="Order total" state="Shopify-controlled" /></div></div><p className="mt-4 text-center text-xs text-stone-500">Representative checkout anatomy — not a live Shopify checkout.</p></section><aside className="space-y-3">{zones.map(zone => <div key={zone.label} className="studio-card p-4"><div className="flex items-start justify-between gap-3"><div><p className="text-sm font-semibold">{zone.label}</p><p className="mt-1 text-xs leading-5 text-stone-500">{zone.detail}</p></div><span className={`shrink-0 rounded-full px-2 py-1 text-[10px] font-bold ${zone.tone === "accent" ? "bg-violet-50 text-violet-700" : "bg-stone-100 text-stone-600"}`}>{zone.state}</span></div></div>)}</aside></div></>;
}

export function CapabilityCenterPanel() {
  const legend = [
    ["Brand-controlled", "Theme tokens and approved brand identity choices."],
    ["Merchant setting", "Shopify setting available after capability validation."],
    ["Extension content", "App-provided module in an eligible Shopify target."],
    ["Shopify-controlled", "Checkout structure or logic that remains unavailable."],
  ];
  return <><div><p className="eyebrow">Capability Center</p><h1 className="mt-2 text-3xl font-semibold tracking-[-0.045em]">A precise boundary for every decision.</h1><p className="mt-2 max-w-2xl text-sm leading-6 text-stone-500">Every control belongs to a single ownership state. Until installation completes, possible Shopify capabilities remain not yet verified.</p></div><div className="mt-6 grid gap-4 md:grid-cols-2 xl:grid-cols-4">{legend.map(([title, copy], index) => <section key={title} className="studio-card p-4"><div className={`grid h-9 w-9 place-items-center rounded-lg ${index === 0 ? "bg-violet-50 text-violet-700" : index === 1 ? "bg-emerald-50 text-emerald-700" : index === 2 ? "bg-amber-50 text-amber-700" : "bg-stone-100 text-stone-600"}`}>{index === 0 ? <Palette className="h-4 w-4" /> : index === 1 ? <BadgeCheck className="h-4 w-4" /> : index === 2 ? <Layers3 className="h-4 w-4" /> : <Lock className="h-4 w-4" />}</div><h2 className="mt-4 text-sm font-semibold">{title}</h2><p className="mt-2 text-xs leading-5 text-stone-500">{copy}</p></section>)}</div><section className="studio-card mt-6 overflow-hidden p-0"><div className="grid grid-cols-[1fr_0.55fr_0.8fr] gap-3 border-b border-stone-100 bg-stone-50 px-5 py-3 text-[11px] font-semibold uppercase tracking-[0.08em] text-stone-500"><span>Control</span><span>Outcome</span><span>Safe alternative</span></div>{[["Logo placement", "Not yet verified", "Preview approved lockups"], ["Global color tokens", "Not yet verified", "Save the theme as a draft"], ["Delivery promise module", "Not yet verified", "Configure representative copy"], ["Payment method order", "Unavailable", "Keep Shopify payment logic unchanged"], ["Customer field placement", "Unavailable", "Use an eligible supporting-content zone"]].map(([control, outcome, alternative]) => <div key={control} className="grid grid-cols-[1fr_0.55fr_0.8fr] gap-3 border-b border-stone-100 px-5 py-4 text-sm last:border-0"><span className="font-semibold text-stone-800">{control}</span><span className={outcome === "Unavailable" ? "text-stone-500" : "text-[#6556d9]"}>{outcome}</span><span className="text-stone-500">{alternative}</span></div>)}</section></>;
}

function MapZone({ title, state }: { title: string; state: string }) { return <div className={`mb-2 rounded-lg border px-3 py-2.5 text-xs last:mb-0 ${state === "Shopify-controlled" ? "border-stone-200 bg-stone-50 text-stone-500" : "border-dashed border-violet-300 bg-violet-50/60 text-violet-800"}`}><div className="flex items-center justify-between"><span className="font-semibold">{title}</span><span className="text-[10px]">{state}</span></div></div>; }
function Stat({ label, value, good = false }: { label: string; value: string; good?: boolean }) { return <div><p className="label">{label}</p><p className={`mt-1 text-sm font-semibold ${good ? "text-emerald-700" : "text-amber-700"}`}>{value}</p></div>; }
